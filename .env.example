# MedTech Radar secrets. Copy this file to .env, fill in values, then restrict it.
#   Linux or n8n host:  chmod 600 .env
#   Windows:            icacls .env /inheritance:r /grant:r "%USERNAME%:F"
# .env is gitignored. Values are referenced by name only, never written into
# scripts, workflows or config files.

# Claude API key used by the extraction and scoring scripts.
ANTHROPIC_API_KEY=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

# Optional. Only needed if the ntfy topic is protected by an access token.
NTFY_TOKEN=
