# Secrets and personal data. Never tracked.
.env
config/profile/

# Personal strategy documents stay local and untracked on purpose.
# The repo has no remote tonight, but if one is ever added these must not travel.
Radar_Project_Instructions.md
Radar_Project_Knowledge_Pack.md

# Overnight reports are for reading, not versioning
MORNING_REPORT.md

# Runtime artefacts
dashboard.html
db/radar.sqlite
db/*.sqlite
db/*.sqlite-journal
db/*.sqlite-wal
db/*.sqlite-shm
test/*.sqlite
test/*.sqlite-journal
test/*.sqlite-wal
test/*.sqlite-shm
test/last_digest.html
test/last_digest.txt
test/last_signal.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# Timestamped database backups taken before schema changes
db/backups/
